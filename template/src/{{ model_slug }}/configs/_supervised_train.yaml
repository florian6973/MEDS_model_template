defaults:
  - datamodule: supervised
  - model: default
  - trainer: default
  - callbacks: default
  - logger: csv
  - optimizer: adamw
  - _self_

max_seq_len: 256
batch_size: 32
num_workers: 0
seed: 0

tensorized_cohort_dir: ???
labels_dir: ???
output_dir: ???
model_initialization_dir: null
do_overwrite: false
do_resume: true

hydra:
  run: {dir: "${output_dir}/.logs"}
  job: {chdir: false}
