# The head trained on frozen embeddings. Selected by supervised_train (`model: probe`), which injects
# `input_dim` from the width of the materialized embeddings. Add your own arguments as you add them to
# `Probe` in src/{{ model_slug }}/model.py.
_target_: {{ model_slug }}.model.Probe
