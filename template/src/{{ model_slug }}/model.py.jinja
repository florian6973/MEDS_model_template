{% set batch_type = 'MEDSTorchBatch' if data_backend == 'mtd' else 'Batch' -%}
"""Your model. **This file is a stub — nothing here is implemented.**

The template generates the *command DAG*, not the model: the ``{{ profile }}`` profile decides which
commands run and how their artifacts connect, and this file is where you say what the model actually does.
Every hook below raises until you write it.

The DAG calls exactly these hooks, and nothing else:
{% if profile != 'packaged' %}
- ``compute_loss(batch) -> (loss, metrics)`` — used by {% if profile in ['finetune', 'probe', 'zero_shot_direct', 'zero_shot_materialized'] %}``pretrain``{% if profile in ['finetune', 'probe'] %} and ``supervised_train``{% endif %}{% else %}``supervised_train``{% endif %}.
{% endif %}{% if profile in ['supervised', 'finetune'] %}- ``predict_step(batch) -> {"predicted_boolean_probability": [B]}`` — used by ``predict``.
{% endif %}{% if profile == 'probe' %}- ``encode(batch) -> [B, D]`` — the representation ``infer`` materializes as embeddings.
- ``Probe`` — the head ``supervised_train`` fits on those frozen embeddings, and ``predict`` scores with.
{% endif %}{% if profile == 'zero_shot_materialized' %}- ``infer_step(batch) -> {"predicted_boolean_probability": [B]}`` with ``inference_kind = "scores"`` —
  ``infer`` materializes these, and ``predict`` reads them back without re-running the model.
{% endif %}{% if profile == 'zero_shot_direct' %}- ``predict_step(batch)`` and ``resolve(...)`` in ``predict.py`` — how a task is answered directly.
{% endif %}{% if profile == 'packaged' %}- ``predict`` in ``predict.py`` — this profile ships its own weights, so there is nothing to train.
{% endif %}
Once you implement them, **delete the ``is_stub`` marker**. The conformance tests in ``tests/`` skip while
it is set and start running when it is gone; they are the contract your model has to satisfy.

{% if data_backend == 'mtd' %}``meds_model_base.lightning.modules`` provides the pieces that must know the MEDS batch format —
``CodeEmbedder``, ``padding_mask``, ``masked_mean``. Everything else is yours.
{% else %}The batches these hooks receive are whatever your datamodule (``datamodule.py``) collates from the
featurized patients artifact — the ``Batch`` alias below is yours to replace along with it.
{% endif %}"""
{% if profile != 'packaged' %}
from collections.abc import Callable

{% if profile == 'probe' %}import lightning.pytorch as L
{% endif %}from meds_model_base.lightning.modules import BaseLightningModule
{% if data_backend == 'mtd' %}from meds_torchdata import MEDSTorchBatch
{% endif %}from torch import Tensor
{% if data_backend == 'custom_featurization' %}
#: Your datamodule's batch type. `object` only marks the stub: replace it (a TypedDict, a dataclass, a
#: plain Tensor — whatever datamodule.py yields) when you implement the model.
Batch = object
{% endif %}{% endif %}{% if profile == 'packaged' %}

class Model:
    """This profile ships trained weights with the repository, so there is no module to train.

    Load them however you like — a checkpoint under ``src/{{ model_slug }}/weights/``, a package resource,
    a download pinned by hash — and use them from ``PredictCommand.predict`` in ``predict.py``.
    """

    #: Delete this once the model is real; the conformance tests skip while it is set.
    is_stub = True
{% else %}

class Model(BaseLightningModule):
    """The model trained by this DAG. Replace every ``NotImplementedError`` below.

    Constructor arguments come from ``configs/model/default.yaml`` via Hydra, plus ``vocab_size``, which
    the training commands inject from ``datamodule.config.vocab_size``. Add whatever else you need to both.
    """

    #: Delete this once the model is real; the conformance tests skip while it is set.
    is_stub = True
{% if profile == 'zero_shot_materialized' %}
    #: `infer` materializes task scores rather than embeddings, so `predict` can read them back directly.
    inference_kind = "scores"
{% endif %}
    def __init__(
        self,
        vocab_size: int,
        optimizer: Callable | None = None,
        scheduler: Callable | None = None,
    ) -> None:
        super().__init__(optimizer=optimizer, scheduler=scheduler)
        self.save_hyperparameters(ignore=["optimizer", "scheduler"])
        self.vocab_size = vocab_size

    def compute_loss(self, batch: {{ batch_type }}) -> tuple[Tensor, dict[str, Tensor]]:
        """Return ``(loss, metrics)`` for a training batch.{% if profile in ['finetune', 'probe'] %}

        This DAG trains twice. ``batch.has_labels`` distinguishes them: pretraining sees no labels, while
        ``supervised_train`` sees ``batch.boolean_value``.
        {% endif %}"""
        raise NotImplementedError("Implement compute_loss in src/{{ model_slug }}/model.py.")
{% if profile == 'probe' %}
    def encode(self, batch: {{ batch_type }}) -> Tensor:
        """Return a ``[B, D]`` representation per prediction timepoint.

        ``infer`` materializes this as the ``embedding`` column; ``Probe`` is then trained on it. Nothing
        downstream re-runs this model, so whatever you return here is the whole interface to the probe.
        """
        raise NotImplementedError("Implement encode in src/{{ model_slug }}/model.py.")
{% elif profile == 'zero_shot_materialized' %}
    def infer_step(self, batch: {{ batch_type }}) -> dict[str, Tensor]:
        """Return ``{"predicted_boolean_probability": [B]}`` — the task scores ``infer`` materializes.

        ``predict`` reads these back from the artifact instead of re-running the model, so the scores stay
        inspectable and reusable.
        """
        raise NotImplementedError("Implement infer_step in src/{{ model_slug }}/model.py.")
{% else %}
    def predict_step(self, batch: {{ batch_type }}, batch_idx: int = 0) -> dict[str, Tensor]:
        """Return ``{"predicted_boolean_probability": [B]}`` aligned to the dataloader's sample order.

        Optionally add ``predicted_boolean_value``. ``predict`` validates the result against
        ``meds_evaluation.PredictionSchema`` and checks that every index timepoint was scored.
        """
        raise NotImplementedError("Implement predict_step in src/{{ model_slug }}/model.py.")
{% endif %}{% endif %}{% if profile == 'probe' %}

class Probe(L.LightningModule):
    """The head trained on frozen embeddings, and the thing ``predict`` actually scores with.

    ``supervised_train`` builds this through ``configs/model/probe.yaml`` and injects ``input_dim`` from
    the width of the materialized embeddings. Batches are plain ``(features, labels)`` tensors — no MEDS
    batch, no tokenization.

    Keep it small: a probe measures what the representation already contains, so capacity here is a
    confound rather than a feature.
    """

    #: Delete this once the probe is real; the conformance tests skip while it is set.
    is_stub = True

    def __init__(self, input_dim: int) -> None:
        super().__init__()
        self.save_hyperparameters()
        self.input_dim = input_dim

    def forward(self, x: Tensor) -> Tensor:
        """Return ``[B]`` logits for a ``[B, input_dim]`` batch of embeddings."""
        raise NotImplementedError("Implement Probe.forward in src/{{ model_slug }}/model.py.")

    def training_step(self, batch, batch_idx: int) -> Tensor:
        raise NotImplementedError("Implement Probe.training_step in src/{{ model_slug }}/model.py.")

    def configure_optimizers(self):
        raise NotImplementedError("Implement Probe.configure_optimizers in src/{{ model_slug }}/model.py.")
{% endif -%}
